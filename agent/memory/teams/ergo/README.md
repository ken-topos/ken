# teams/ergo — Ergonomics-team lessons

Loaded by the Ergo ring — `ergo-leader`, `ergo-implementer`, `ergo-qa` — in
addition to `fleet`, `build/`, and the function scope (`build/leaders` ·
`build/implementers` · `build/qa`).

For lessons specific to the ergonomics surface: diagnostics, error rendering,
formatter behaviour, and the developer-facing edges of the toolchain.

| Lesson | One-line |
|---|---|
| _(none recorded yet)_ | |

**An empty scope is a normal state, not a defect** — this ring has not yet paid
for a team-specific lesson. Record one at the broadest scope where every reader
must apply it: a lesson about authoring a pin belongs in
`agent/playbooks/tools/pin-a-property.md`, one about review or coordination flow
in `fleet/` or `build/`, and only a genuinely Ergo-specific one here.
