# teams/verify — Verify-team lessons

Loaded by the Verify team (leader / implementer / QA), in addition to `fleet`,
`build`, and the role scopes. Verify-specific lessons about gates, registries,
attestation ledgers, and the discipline of proving a mechanism binds rather than
asserting that it does.

| Lesson | One-line |
|---|---|
| [a-claimed-executable-inventory-needs-a-reversible-deletion-proof](a-claimed-executable-inventory-needs-a-reversible-deletion-proof.md) | A claimed executable inventory needs a reversible deletion proof that fails at its stored reference |
