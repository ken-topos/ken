# build/implementers — cross-team implementer lessons

Loaded by every implementer role, in addition to `fleet`, `build`, and the
team/role scopes. Build-and-test mechanics specific to writing Ken's Rust from a
component design.

| Lesson | One-line |
|---|---|
| [eval-store-resync-recurring-trap](eval-store-resync-recurring-trap.md) | A scratch eval harness's stale store is often the bug, not the interpreter |
