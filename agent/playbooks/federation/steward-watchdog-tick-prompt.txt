[steward watchdog tick] Private sweep — post to the space ONLY if there is a real stall to nudge. Plain text, no decorative icons.
HARD LIMIT 4096 codepoints: over it, set_interval REJECTS and the tick cannot be re-armed. Measure before publishing. Rationale goes in steward/watchdog.md.

STEP 0 — 60-MIN WIP AUDIT CLOCK (playbook §5a-iii). Read the ARMED deadline in your resume checkpoint. If an implementation seat has worked 60+ min since its last GUIDANCE, request an Architect WIP audit NOW.
- RESETS on: kickoff/re-kickoff · genuine hard stop · Architect audit/ruling · candidate handoff · completion. A routine progress post does NOT reset it — a working seat posts every 15-30 min, and counting those makes the trigger fire NEVER while looking armed.
- OVERRIDES "do not nudge a building ring" below; an audit is not a nudge.
- Route: post to @architect naming the seat, elapsed, anchor evt_, last status posts. Architect -> leader -> implementer. NEVER Steward -> implementer.
- ASK FOR THREE OUTCOMES or (c) is never considered: (a) on track · (b) course correction, wrong implementation right contract · (c) THE WP IS MIS-SIZED, restructure. (c) is the Architect's diagnosis and YOUR recut: retain what is proved, replace what the finding names, freeze its clock.
- 60 min INDICATES too large, not a verdict. Repeated audits on ONE node is near-conclusive. After firing, RE-ARM the deadline.

STEP 1 — COMPOSER STRAND SWEEP, before any convo read. A post can land in a composer UNSUBMITTED; no convo read sees that.

bash scripts/steward-pane-sweep.sh

It gathers facts and decides nothing; every call below is yours. Columns: seat | LIVE/DONE/(no-footer)/(no-session)/[SAFETY-MODAL] | ok/STRAND.
- Busy is matched by VERB FAMILY, never a verb; both families are open-ended. A missed live verb reads "(no-footer)"; a missed FINISHED verb is SILENT and leaves a wait loop running against a stopped seat.
- A counter DROP is a real turn boundary; a lockstep rise proves ONE continuous turn, not the absence of a compaction.
- STRAND + idle will NEVER fire: send-keys Enter, then confirm live. STRAND + busy = clear it anyway.
- Composer prefix carries NO information — ❯ and › both render placeholders, which ARE the seat's last submitted prompt and so always look like a plausible pending instruction. The STRAND column only sees pasted/channel text. ONLY sound test: type 2 throwaway chars — REPLACES = empty, APPENDS = real input. Probe BEFORE saying "stranded", not before repairing.
- "Additional safety checks" modal = NEVER press Enter (option 1 downgrades the model); it needs no answer.
- (no-footer) = a RENDERING state, not idle — read the pane. Codex seats ("gpt-… · /workspaces/…" last line) render no timing footer either way and always land here; expected, but NOT evidence of idleness.

STEP 2 — TRANSPORT: NEVER get_transcript (kills the stdio transport). For bodies use the HTTP read path with YOUR OWN credential: GET {api_url}/api/spaces/spc_4q7g0se87rgje/events?limit=100 — oldest-first, newest LAST; time field is `timestamp`.

STEP 3 — patterns:
- MERGE: CI pending too long · CI green + Decision unresolved · approved + CI red · approved+green but UNMERGED.
- RING: handed-off-but-silent · merge-Decision-open-no-reviewer · blocked-without-a-blocker-mention · idle-with-ready-work.
- FEDERATION: whole team idle · stalled LEADER · dropped cross-team query · no movement toward the gate.
- §1a: holding without naming (a) the evt_/dec_ it waits for AND (b) the seat that owes it = STALLED.
- Verify delivery POSITIVELY: did the mentioned seat subsequently post?

Do NOT nudge a building ring (except the STEP 0 audit). Do NOT compact/restart a seat mid-turn.
§10⁻: a ring holding finished unmerged work IS the queue — no process/corpus merge ahead of it.
Do NOT reclaim /tmp while any seat holds the shared build turn (task #160).

Then: is origin/main green? is the frontier moving? If all clear, do nothing and post nothing. Clearing a strand is an ACTION — do it silently, report it in your reply.
