---
id: F3-39
title: "F3 [task-list #39] — reducer: degrade-not-wrap + retire legacy arms"
status: draft
owner: runtime
size: TBD
gate: none
depends_on: []
blocks: [RT-SPLIT]
github: null
origin: gap analysis — delivered state vs. research/linux-abi-ii-work-program-proposal.md (pre-restart Steward task list)
---

> **On the `#N`:** this is an index from a **pre-restart Steward task list**,
> **not a GitHub issue number.** The list was derived from the gap between what
> was actually delivered and what
> `research/linux-abi-ii-work-program-proposal.md` assumes was already done.
> Sibling entries `#38`/`#32`/`#24`/`#25` carried nothing but a number and were
> **dropped** (operator, 2026-07-21). Earlier issue files asserted these as
> `github:` references; that was a **false provenance claim** and is corrected.


Tracked in the current tracker as `F3 / F2/F3 reducer`. Closes two
ratified correctness defects in the interpreter's primitive reducer
(`crates/ken-interp/src/eval.rs`, `prim_reduce`), both already adjudicated
3-way in Phase-1 and specified in `spec/10-kernel/18a-primitive-registry.md`
§5 (the F1–F5 severity+AC table). Cites ADR 0009 (capability-supply:
bracket-the-untrusted; wrong-value fix in the tested-not-trusted `ken-interp`
ring, not a kernel change) — same posture as the already-merged `WP F1`
(bignum Int, `bb40654`) and the Decimal/Char demotes (`4eea2072`).

Must precede `RT-SPLIT` (the `cranelift_backend.rs` decomposition) per the
current tracker's sequencing.

Full brief: [`docs/program/wp/F2F3-reducer-degrade.md`](../wp/F2F3-reducer-degrade.md).
