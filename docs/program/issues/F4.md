---
id: F4
title: "content-addressing + value-model design (aka PX8-F-PROOF)"
status: draft
owner: foundation+spec-enclave
size: M
gate: none
depends_on: [A3]
blocks: []
github: null
origin: steward frame (shovel-ready brief)
---

> ## ⛔⛔ STOPPED 2026-07-27 — `SPEC-STORE-SPLIT` RETIRED the frame's fixed inputs
>
> ⛔ **Do not release, do not elaborate, do not queue `A3` in front of it.**
> `SPEC-STORE-SPLIT` (merged `c631841d`) demoted in-process hashing, interning,
> slot identity, probing policy and the store index to **private runtime
> choices** — and this node's whole subject is content-addressing *as a value-model
> requirement*. Its frame's `OQ-hash`, `OQ-7` and `OQ-witness` rows are dead, and
> its mandated design outline requires encoding **closures by code-pointer/id +
> captured-env hash**, which `41 §2.1` now **forbids**.
>
> ⭐ **The frame's §0 told the reader not to re-open those inputs**, so a faithful
> team would have read the prohibition as authority not to check. The full
> row-by-row comparison against the landed contract is in the stop banner of
> [the frame](../wp/F4-content-addr-design.md).
>
> ⇒ **Owed: a Steward re-cut** deciding whether a node about content-addressing
> still has a subject once content-addressing is private, and if so what it is.
> ⛔ Not an edit — the premise moved, not the wording.

Content-addressing + value-model design. Feeds `K3` (content-addressed value
model) and `X2` (runtime hardening) per `05-implementation-dag.md`. Referred
to in the current tracker interchangeably as `F4` and `PX8-F-PROOF`.

**Pipeline status:** Steward frame → awaiting spec-leader elaboration → Team
Foundation. Per the WP release process, the spec-leader elaborates it to full
team-ready rigor (`/spec 40-runtime/41,44` + `conformance/runtime/` detail) on
its branch before Foundation is released to build it; Foundation must not
start from the Steward's frame alone.

Blocked behind `A3` (the catalog-coverage walker) — `A3` is queued first
because this WP edits the catalog `A3` covers. Also carries adversary finding
S3 per the decision log; risky, may hard-stop to Spec.

Full brief: [`docs/program/wp/F4-content-addr-design.md`](../wp/F4-content-addr-design.md).
