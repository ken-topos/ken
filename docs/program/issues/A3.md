---
id: A3
title: catalog-coverage walker
status: draft
owner: TBD
size: TBD
gate: none
depends_on: []
blocks: [F4]
github: null
origin: TBD
---

> ## ⛔ ON HOLD — its only dependant is stale. Steward, 2026-07-27
>
> ⛔ **Do not frame or assign this as idle-team filler.** `A3` feeds **no gate**
> and `blocks: [F4]` is its **only** dependant, so its entire urgency is derived
> from `F4` — which `SPEC-STORE-SPLIT` (merged `c631841d`) made stale and which
> is now stopped pending a Steward re-cut. See the stop banner in
> [`docs/program/wp/F4-content-addr-design.md`](../wp/F4-content-addr-design.md).
>
> ⚠ **Why this is worth a banner rather than silence:** `A3` is one of the few
> nodes with `depends_on: []`, so a sweep for *"what can an idle team start?"*
> surfaces it near the top — exactly when a team is idle and the pressure to
> release something is highest. **Zero dependencies is not the same as live
> demand**, and this node is the case where the two come apart.
>
> ⇒ Re-read `A3` **after** the `F4` re-cut decides whether that node still has a
> subject. If `F4` does not survive, `A3` has no dependant at all and should be
> retired rather than framed.

Referred to in the current tracker only as "A3 (catalog-coverage walker)",
queued ahead of `F4` because `F4` edits the catalog A3 covers. No WP brief
exists yet under `docs/program/wp/`; owner and size are not yet assigned
(tracker: `*TBD*`) — do not guess. Tooling item; feeds no gate.
