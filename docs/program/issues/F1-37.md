---
id: F1-37
title: "F1 [task-list #37] — bignum Int soundness review for K3 trusted-base promotion"
status: draft
owner: runtime
size: TBD
gate: none
depends_on: [RT-PARITY]
blocks: []
github: null
origin: evt_55tnpnhvcpptf
---

> ## STATUS CORRECTED `ready` TO `draft` — 2026-08-08, Steward
>
> **`ready` means shovel-ready: a written frame, dependencies merged.**
> This node was not, because
> no frame exists, and its `depends_on` names `RT-PARITY`, which is
> `closed` (resolved without landing) rather than merged.
>
> **The correction is not a downgrade of the work.** A node advertising
> startable work it does not have makes the backlog read deeper than it
> is, and that depth is exactly what a Steward reads to decide whether a
> team is idle for want of work or for want of a lane.

> **On the `#N`:** this is an index from a **pre-restart Steward task list**,
> **not a GitHub issue number.** The list was derived from the gap between what
> was actually delivered and what
> `research/linux-abi-ii-work-program-proposal.md` assumes was already done.
> Sibling entries `#38`/`#32`/`#24`/`#25` carried nothing but a number and were
> **dropped** (operator, 2026-07-21). Earlier issue files asserted these as
> `github:` references; that was a **false provenance claim** and is corrected.


Tracked in the current tracker as `F1`: re-classified soundness-relevant
per research `evt_55tnpnhvcpptf`, routing to the Architect now that RT-PARITY
(its release gate) has resolved. Feeds the eventual **K3** trusted-base
promotion indirectly, via the ratified Phase-2 tranche — the arbitrary-
precision `Int` work (`WP F1`, merged `bb40654`) is that tranche's dependency
root, and this item is the soundness sign-off gating its promotion into the
trusted base.

No dedicated WP brief exists yet for this item under `docs/program/wp/` — it
has not been reconstructed into a full frame. Owner is recorded as `runtime`
per the current tracker's status table; do not read this as a sized,
kicked WP. `depends_on: [RT-PARITY]` reflects the historical release gate
(now merged, hence `status: ready`).

No `gate` (G-number) is cited against this item in `05-implementation-dag.md`;
it feeds the `K3` DAG node, not a named gate.
