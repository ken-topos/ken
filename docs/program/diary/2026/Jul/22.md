# 2026-07-22 — RT-SPLIT slices 3–5, the doc-currency outage, and three
# silent-stall classes

Superseded live-state blocks moved out of
[`CURRENT-BRIEFING.md`](../../CURRENT-BRIEFING.md) as they stopped being true.

## ✅ RESOLVED — `origin/main` was RED on its own documentation gate

**Live from `835ce1c4` until the third fold landed.** `DOC-CURRENCY-ANCHOR`
merged (PR #843) with `library/REVISION = cc2af484`, a **pre-squash branch
commit**. `main` merges are **squash** merges, so that commit was **not
reachable from `main`**:

```
$ git merge-base --is-ancestor cc2af484 origin/main   → FAILS
$ bash scripts/gen-doc-status.sh --check
gen-doc-status: library/REVISION '...' is not an ancestor of the current tree
```

Found by the **librarian** and independently by the **adversary**.

**★ The mechanism was structurally incompatible with the merge model, and
nothing in the WP could see it** — on the branch, in every worktree, at every
review, `cc2af484` *is* an ancestor. **The property only becomes false at the
moment of merge, which is after the last check anyone runs.** Three folds, an
Architect approval, a Librarian QA pass and a green CI run all held.

**Closed with three folds, each fixing a genuinely different defect** — the
detail is preserved in the closure record in
[`issues/DOC-CURRENCY-ANCHOR.md`](../../issues/DOC-CURRENCY-ANCHOR.md):
`REVISION` naming a pre-squash commit; the regression driving a **synthetic
fixture** instead of the repo's real `REVISION`; and the check **silently
skipping itself** when `origin/main` was unresolvable. The third was found
only by running a probe that had been offered as skippable.

## ✅ SUPERSEDED — build-track state through slice 2

Slice 1 merged `@ 0f9a483a` (PR #834); slice 2 (`planning`) `@ dd838f0f`
(PR #838). Slice 3 was gated on the Architect's §10.4a seam ruling
(`evt_725trc8dfag3c`): the visibility budget forced an **encapsulated seam**,
since a literal `CompiledModule` field extraction cost 12 and would have
reached **25 of a 24 cap** before slices 5–7 started. Required shape was a
single transparent `pub(super) fn from_parts(…)` at the three existing
construction sites — **9 seams, not 12.** All of this is transcribed durably
in the frame; slices 3, 4 and 5 have since landed at **22/24**.

## ✅ SUPERSEDED — doc-track state during DOC-CURRENCY-ANCHOR QA

Candidate `wp/DOC-CURRENCY-ANCHOR @ 139f1442` in Librarian QA, third fold:
`library/*` blanket exclusion narrowed to a visible per-document-kind rule,
symlink sources rejected by tracked git mode at both `REVISION` and `HEAD`,
duplicate/out-of-order `kind`-field desync closed. 17/17 green.

## ★ THE LIBRARIAN STALLED ON A PROVIDER CONTENT REFUSAL — durable

~04:47Z. Mid-QA, running a **parser-differential** check (does the awk shell
implementation agree with the Rust one on where a `library/manifest.toml`
record ends, given a `kind` token at column zero inside a multiline `sources`
array). The provider classifier read it as a cybersecurity request, **the turn
ended, and the seat posted nothing** — `doc-author` was waiting on a mention
that would never arrive. Only evidence was the pane tail.

**Second occurrence in two days on `gpt-5.6-sol`** (first: `runtime-
implementer` on a native differential harness). **The trigger is the SHAPE of
adversarial QA, not the domain** — *make one implementation disagree with
another and demonstrate the bypass* is how a differential conformance test is
written and also how a parser-confusion attack is written.

⚠ **The doc ring is maximally exposed**, because planted-violation and
mutation-proof work is its *core method*.

**Recovery that worked (one attempt, not repeated evasion):** rouse with an
accurate re-description of the legitimate work, **plus an explicit escape** —
if it will not proceed, report the case as an **explicitly unverified
residual**, name the exact case, and approve or reject on the rest.

> ⛔ **Corrected the same day: the escape is a FALLBACK, not an equal option.**
> Told the librarian an unverified residual would be acceptable; it instead ran
> the probe and found a **confirmed false green** — fold 3 above. **Provider
> refusals are selective against fail-open detection**, so a blocked probe is
> likelier than average to be sitting on a real defect. Offer the escape, but
> ask for the probe first.

## ★ I ASSIGNED A STEWARD-ONLY DUTY TO A SEAT — TWICE THIS WEEK

Told `runtime-leader` to *"run the handoff gate first"* before slice 3. Under
§15 the gate is **mine** — leaders don't `moot compact`. They bounced it back,
correctly, 38 seconds after kicking off. Earlier I told `doc-author` to push
their WP branch; they accepted, then tested it and hit `could not read Username
for 'https://github.com'`.

**The tell both times: I wrote an instruction whose verb belongs to a
capability I hold and the recipient doesn't** — *push, compact, merge, mint*. A
duty assigned to a seat that cannot perform it is **worse than the open gap,
because everyone then believes it is handled.** A QA/leader seat may carry a
**detection** item; the **remedy** always routes to me.
