# Q2b triage result — ergo-qa

23 tests / 8 files. List only — no test edits, no PR. See `README.md` for
format and `agent/playbooks/build/qa.md` for the classification rule.

| test | class | confidence | note |
|---|---|---|---|
| `nc14_data_match_lowering.rs::user_data_two_payload_binders_preserve_de_bruijn_order` | durable-invariant | high | Name numeral names the constructor arity under test (2-field ctor), not a census; the property (payload-binder de Bruijn order preserved through match lowering) generalizes to any arity — an added 3-field case would be a new test, not a break to this one. |
| `v1_acceptance.rs::requires_elaborates_to_pi_proof_arg` | durable-invariant | high | Fixed 2-param fixture; `params.len()==3` is the exact structural contract for *this* function (n, d, proof), not a growing count. |
| `v1_acceptance.rs::ensures_emits_obligation_not_sigma` | durable-invariant | high | Fixed fixture; one `ensures` clause → one obligation is the spec's own per-clause mapping, plus a structural `assert_eq!` on the emitted core term (not success-only). |
| `v1_acceptance.rs::non_omega_predicate_surface_error` | durable-invariant | high | Rejection property is durable (a Type-sorted proposition used as `requires` stays rejected under any intended extension); assertion is broad (`is_err()`, no `ElabError` variant named) — a real-but-weak invariant, tightening the variant check is Q3+ rework, not a reclass. |
| `v1_acceptance.rs::result_in_ensures_resolves` | durable-invariant | high | Fixed fixture, one ensures clause → one obligation; same per-clause mapping as `ensures_emits_obligation_not_sigma`. |
| `v1_acceptance.rs::result_out_of_ensures_rejects` | durable-invariant | high | Scope-rejection property is durable; broad `is_err()` (no variant named) — same caveat as `non_omega_predicate_surface_error`. |
| `v1_acceptance.rs::old_resolves_in_space_op_ensures` | durable-invariant | high | Fixed fixture, one obligation for one ensures clause in a `space proc`; consistent per-clause mapping. |
| `v1_acceptance.rs::old_out_of_scope_rejects` | durable-invariant | high | Scope-rejection (soundness) property is durable; broad `is_err()` — same variant-naming caveat as the two siblings above. |
| `v1_acceptance.rs::obligation_hole_set_exposed_to_v2` | durable-invariant | high | Fixed fixture: two distinct `ensures` clauses → two distinct holes, both open. Tests the V1→V2 interface contract, not a growable count. |
| `v1_acceptance.rs::prove_goal_obligation_and_postulate_binding` | durable-invariant | high | Fixed fixture with a full discharge round-trip (open → cert → closed), not just success; the `addComm`-is-the-hole-postulate identity is a structural assertion, not a literal. |
| `v1_acceptance.rs::law_all_omega_fields_is_proposition` | durable-invariant | high | Fixed fixture: one `law` with two Ω-typed fields → two obligations, one per field — the spec's own per-field mapping, not a frozen count. |
| `v1_acceptance.rs::requires_on_first_of_two_params` | durable-invariant | high | Named regression test for a fixed de Bruijn shift bug (V1-fix, non-final `requires` param); "first_of_two" names the regressed scenario, not a stale quantity — asserts the exact `Lam` chain structurally. |
| `i4_program_caps_step1.rs::program_caps_is_authority_parametric_and_adds_zero_trust` | durable-invariant | high | Structural shape of `ProgramCaps` (1 constructor, param/arg shape) is a fixed contract fact, not a growing census; the zero-trust-delta check is a real before/after set comparison, not a raw count. |
| `i5_scoped_capability.rs::traversal_denies_while_sibling_accepts_through_real_dispatch` | durable-invariant | high | `fs_trace().len()` 0-vs-1 is the discriminator proving the denied traversal never reached real dispatch while the sibling did — a load-bearing 0/1 delta, not a frozen count. |
| `i5_scoped_capability.rs::posix_resolution_is_descriptor_relative_and_nofollow` | durable-invariant | high | `SystemTime::now()`/`temp_dir()` are used only to make a unique scratch directory name for test isolation, not as a timing oracle — the flagged pattern is incidental; the actual property (NoFollow + descriptor-relative resolution rejects an escaping symlink) is a stable POSIX-host invariant. |
| `check.rs::let_check_accepts_valid_body_at_outer_expected_type` | durable-invariant | high | Fixed `bool_nat_env` fixture; checks a `let` body checks against the outer expected type — broad `is_ok()` but the property (accept) is durable and paired with a sibling reject test in the same file. |
| `check.rs::let_check_preserves_check_mode_for_intro_body` | durable-invariant | high | Same fixture; checks that a `let`-bound lambda body still checks in checking mode against a Pi type — durable property, broad `is_ok()` outcome. |
| `check.rs::universe_no_type_type` | durable-invariant | high | Directly encodes the no-Type:Type discipline (AC-1): `Type 0 : Type 1` accepted, `Type 0 ⇐ Type 0` rejected. This is a foundational soundness invariant, not a snapshot. |
| `check.rs::k2_omega_formation` | durable-invariant | high | Encodes `Ω_l : Type (suc l)` per spec `16 §1.1`; a fixed universe-formation fixture, not a count. |
| `ds6a_int_deceq_certificate.rs::trusted_base_delta_is_exactly_the_two_certificate_postulates` | durable-invariant | high | Uses a real before/after `trusted_base()` set-difference compared against the expected `{sound, complete}` set (plus a `Decl::Opaque` provenance check on each) — exactly the relation/set-equality pattern the playbook recommends over a raw count; "two" in the name is the fixed contract shape of one certificate, not a growing census. |
| `sct_completeness_nested_split.rs::two_level_nested_split_flat_sibling_recursion_accepts` | durable-invariant | high | Soundness-accept discriminator for a specific nested-split shape (checklist item 4); broad `is_ok()` but the fixture and its sibling reject-test below form a matched accept/reject pair. |
| `sct_completeness_nested_split.rs::recursing_on_a_deferred_ih_slot_stays_rejected` | durable-invariant | high | Documented tripwire for a specific slot-collision failure mode (deferred IH slot must never be treated as a field); broad `is_err()` but the property and its adversarial construction are durable and well-motivated in-file. |
| `ir.rs::seed_examples_are_observation_limited` | transition-sentinel | low | `examples.len()==5` is a raw count of a seed list under active growth (the file is titled around "nc5_seed_examples"), and the `match` over `RuntimeObservation` is already exhaustive by construction (only `Returned`/`Trapped` variants exist) — so the match itself cannot fail regardless of count, and the only live assertion is the len literal. This is the exact "milestone census frozen as invariant" anti-pattern the QA playbook names as its motivating failure (a `ken-verify` catalog test asserting an exact op count went red on a legitimate promotion). If the seed count is expected to grow, this needs a `[placeholder — reifies when seed #6 lands]`-style label and a name that says so; as it stands it silently freezes today's seed size. Recommend the crate owner (Runtime/Foundation, not routed to my ownership) either relabel it as a documented sentinel or drop the count assertion in favor of a non-empty + exhaustive-match check. |
