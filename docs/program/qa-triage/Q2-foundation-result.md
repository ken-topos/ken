# Q2b triage result — Team Foundation

Combined output of the Foundation ring (leader + implementer + QA) triaging
the 71-test / 25-file queue in [`Q2-foundation.md`](Q2-foundation.md), per
[`README.md`](README.md). Reading pass only — no test edited, no build run.

Counts: **65 durable-invariant, 1 compat-vector, 2 transition-sentinel,
3 UNCLASSIFIABLE** (71 rows total).

## foundation-leader (24 tests)

| test | class | confidence | note |
|---|---|---|---|
| `crates/ken-cli/src/lib.rs` :: `exit_code_mapping_is_total_and_failure_zero_fails_closed` | durable-invariant | high | Total mapping over success/failure ctor ids with fail-closed default (payload 0→1); exercises representative payloads, not a growing count |
| `crates/ken-cli/src/lib.rs` :: `root_admission_precedes_host_leaves_and_marker_flips_the_same_program` | durable-invariant | high | Effect-trace-length checks confirm root check precedes any host effect and exactly the declared effect runs after admission; not a derived/growing count |
| `crates/ken-cli/src/lib.rs` :: `non_root_proceeds_and_process_input_cannot_forge_the_marker` | durable-invariant | high | Security invariant: only the real UID drives the root marker; process-supplied args/env cannot forge it |
| `crates/ken-cli/tests/px7p_constructor_field_composition.rs` :: `dynamic_carrier_producer_payload_reaches_linked_process_exit` | durable-invariant | high | Exact linked-native effect trace (len 2, ordered fs-error then console-write) for one fixed program; count is exhaustive, not derived |
| `crates/ken-cli/tests/rt_parity_native.rs` :: `output_dir` | UNCLASSIFIABLE | high | Not a `#[test]` — a private helper building a unique tmp-dir name; `SystemTime::now()` is used only for name uniqueness, no assertion depends on wall-clock. Scanner flagged a non-test helper |
| `crates/ken-elaborator/src/lossless.rs` :: `partition_rejects_gap_and_overlap` | durable-invariant | low | `PartitionError` is a struct (no variant to name), but the test only checks `is_err()` for both the gap and overlap cases — doesn't assert on `expected_offset`/`found` to confirm the two shapes are actually distinguished internally |
| `crates/ken-elaborator/tests/dependent_match_nonnullary_acceptance.rs` :: `ac1_list_hypothesis_threading_elaborates_and_narrows_structurally` | durable-invariant | high | Structural check on the elaborated `Term::Elim`'s per-branch domain, not mere kernel-acceptance; `methods.len()==2` is List's fixed ctor arity |
| `crates/ken-elaborator/tests/dependent_match_nonnullary_acceptance.rs` :: `ac1_tree_two_recursive_fields_elaborates_and_narrows_structurally` | durable-invariant | high | Same structural-narrowing check, Tree/2-recursive-field case |
| `crates/ken-elaborator/tests/dependent_match_nonnullary_acceptance.rs` :: `ac2_mis_narrowed_cons_arm_stays_kernel_rejected` | durable-invariant | low | Genuine soundness negative (mis-narrowed arm) but asserts bare `is_err()`; would be stronger naming the specific rejection variant |
| `crates/ken-elaborator/tests/dotted_module_path_parser.rs` :: `selective_import_accepts_a_dotted_module_path` | durable-invariant | high | `items.len()==2` derives from the literal 2-item import list in the test's own source, not a growing set |
| `crates/ken-elaborator/tests/dotted_module_path_parser.rs` :: `module_decl_accepts_a_dotted_module_path` | durable-invariant | high | `decls.len()==1` derives from the literal one-decl module body in source |
| `crates/ken-elaborator/tests/i4d_boundary_header_parser.rs` :: `boundary_keywords_are_reserved_and_four_program_shapes_are_independent` | durable-invariant | high | "Four" names the fixed grammar's boundary-keyword shape combinations, not a derived/growing count |
| `crates/ken-elaborator/tests/l2_acceptance.rs` :: `unknown_ctor_in_pattern_is_error` | durable-invariant | low | Bare `is_err()` for an unknown-constructor-in-pattern rejection; should assert the specific `ElabError` variant to confirm the rejection reason |
| `crates/ken-elaborator/tests/l2_acceptance.rs` :: `data_two_arg_ctor_match_accepted` | durable-invariant | high | "two_arg" names the fixed test-declared `Pair` constructor's arity (representative >1-arity case), not a growing count |
| `crates/ken-elaborator/tests/l3_strings_surface_acceptance.rs` :: `list_floor_recursion_in_sct_sound_zone` | durable-invariant | low | Genuine SCT/termination soundness boundary (non-decreasing self-application rejected) but only asserts bare `is_err()`; recommend naming the termination-rejection variant given this is soundness-adjacent |
| `crates/ken-elaborator/tests/l3b_acceptance.rs` :: `user_deceq_instance_keys_map_via_real_search` | durable-invariant | high | Positive path asserts `is_ok()` (fine); negative path asserts the specific `ElabError::NoInstance` variant — well-discriminated |
| `crates/ken-elaborator/tests/l3b_acceptance.rs` :: `user_ord_instance_drives_verified_sort` | durable-invariant | high | Same pattern (positive `is_ok()`, negative `NoInstance` variant), Ord axis |
| `crates/ken-elaborator/tests/l3b_acceptance.rs` :: `user_deceq_keyed_map_canonical_identity` | durable-invariant | high | Discriminating pair (DecEq-only accepts, Ord-only-without-instance rejects `NoInstance`) proving Map identity is DecEq-keyed, not Ord-keyed |
| `crates/ken-elaborator/tests/l_resolver_globals.rs` :: `ac1_cross_decl_lowercase_global_resolves` | durable-invariant | high | Positive-path `is_ok()`; paired with unflagged `ac1_absent_name_still_errors`, which does assert specific variants |
| `crates/ken-elaborator/tests/l_resolver_globals.rs` :: `ac1_two_decl_chain` | durable-invariant | high | `assert_ne!` on resolved ids, not a count; verifies distinct successful resolutions in a 2-step chain |
| `crates/ken-elaborator/tests/l_resolver_globals.rs` :: `ac3_uppercase_con_still_resolves` | durable-invariant | high | Regression smoke test, positive `is_ok()` |
| `crates/ken-elaborator/tests/l_resolver_globals.rs` :: `ac3_type_position_global_unchanged` | durable-invariant | high | Regression smoke test, positive `is_ok()` |
| `crates/ken-elaborator/tests/match_arm_glyph.rs` :: `match_arm_spellings_share_one_token` | durable-invariant | high | Grammar-spelling-equivalence pinned by exact token stream; both accepted and retired spellings checked |
| `crates/ken-elaborator/tests/match_arm_glyph.rs` :: `match_arm_spellings_parse_and_retired_spellings_reject` | durable-invariant | high | Bare `is_err()` acceptable here — the negative is "does this syntax parse at all," no ambiguity about rejection reason |

## foundation-implementer (24 tests)

| test | class | confidence | note |
|---|---|---|---|
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `postcondition_emits_substituted_goal` | durable-invariant | high | `len()==1` encodes §2.2's one-ensures→one-obligation relationship, not a census; real assertion is phi/goal_closed/provenance shape |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `refinement_return_type_emits_phi` | durable-invariant | high | Same one-clause→one-obligation relationship (§2.1); count is inherent to the single-clause input |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `prove_and_law_emit_one_obligation_per_goal` | durable-invariant | high | Asserts N-fields→N-obligations directly against the declared law fields (`ids.contains` per field name), not an unrelated frozen count |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `present_cert_yields_zero_new_obligations` | durable-invariant | high | Real state-transition invariant: `is_open_hole` flips true→false on discharge; `open_count==0` follows from the discharge itself |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `trivial_clause_still_emits_obligation` | durable-invariant | high | Explicit counter-rule test: emission is keyed on clause presence, never a triviality heuristic — guards exactly the "helpful optimization" failure mode |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `match_branch_gamma_scrutinee_equation_placeholder` | transition-sentinel | high | Honestly labeled `[placeholder — reifies in V3]`, names the retiring event (Elim walking + Eq-typed scrutinee equations); current body is a baseline smoke check |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `let_binding_adds_equation_to_gamma_placeholder` | transition-sentinel | high | Same shape, honestly labeled placeholder retiring at V3 (Let walking + Eq-typed Γ equation) |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `provenance_and_stable_ids` | durable-invariant | high | Real id-stability/provenance contract (consecutive stable ids, kind+index, stable across re-extraction); `len()==2` is incidental to the two-clause input |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `decoupled_from_sigma_sort` | durable-invariant | high | Core assertion is phi/goal_closed are never Sigma-projections/Sigma-typed (§1.1 erratum decoupling); `len()==1` is setup |
| `crates/ken-elaborator/tests/v2_acceptance.rs` :: `v2_enriches_v1_obligations_with_context` | durable-invariant | high | Checks goal_closed=Pi(context,phi) consistency, hole_id continuity with V1, id-prefix naming — real structural invariants |
| `crates/ken-interp/src/eval.rs` :: `mismatch_stops_before_interpreter_host_entry` | durable-invariant | high | Fail-closed ABI-mismatch gate: mismatched artifact never enters host boundary (`entered` stays false); matching hash proceeds |
| `crates/ken-interp/src/eval.rs` :: `posix_whole_file_create_or_keep_preserves_an_already_appeared_leaf` | durable-invariant | high | `SystemTime::now()` only names a unique scratch tmp dir, not a timing assertion; real assertion is CreateOrKeep never overwrites a competitor-created leaf |
| `crates/ken-interp/src/eval.rs` :: `px1_non_linux_fs_driver_fails_before_host_io` | durable-invariant | low | `#[cfg(not(target_os = "linux"))]`-gated; the fail-fast-before-host-io invariant is real, but if CI only runs Linux this body never executes — flagging reachability, out of scope for this pass |
| `crates/ken-interp/src/eval.rs` :: `actual_raw_requests_survive_descriptor_collision` | durable-invariant | high | `events.len()==2` follows directly from the two `dispatch_read` calls made; real check is dot-segment-normalized paths don't collapse into one trace entry |
| `crates/ken-interp/src/eval.rs` :: `recording_is_behaviorally_inert` | durable-invariant | high | Core property: recorder presence must not alter interpreter outcome/trace/denials (plain==observed); `len()==1` follows from the one dispatch performed |
| `crates/ken-interp/src/eval.rs` :: `resource_table_lifetime_is_owned_by_one_interpreter_invocation` | UNCLASSIFIABLE | high | Asserts over `eval.rs`'s own source text (`include_str!` + string splitting) as a proxy for "one ResourceTableV1 per invocation," not runtime behavior — a compatible refactor (rename/split helper) flips it red with zero regression. Needs a real behavioral test before it can be classed durable-invariant |
| `crates/ken-interp/src/eval.rs` :: `f1_zero_and_sign_canonical` | durable-invariant | high | Canonical-zero-representation invariant (exactly one zero limb, canonical sign) — real content-addressing/dedup soundness property, not a magic-number freeze |
| `crates/ken-interp/tests/i2_console_floor.rs` :: `console_surface_and_package_are_zero_trust_definitions` | durable-invariant | high | Before/after `trusted_base()` set-equality + no-Axiom source scan — genuine TCB-accounting invariant; "zero" is a real design commitment (Console stays ordinary checked Ken), not a frozen count |
| `crates/ken-interp/tests/i2_console_floor.rs` :: `package_helpers_route_exact_bytes_and_one_newline` | durable-invariant | high | print_line/eprint_line always append exactly one newline unconditionally (even when input already ends in one) — real always-append contract |
| `crates/ken-interp/tests/i3_fs_floor.rs` :: `surface_and_render_package_add_zero_trusted_base_entries` | durable-invariant | high | Same zero-trust-base pattern as the Console test, for the FS package |
| `crates/ken-kernel/tests/k4_omega_motive.rs` :: `type_motive_regression_still_admitted` | durable-invariant | high | Regression guard: Ω-motive support addition must not break the existing Type-codomain accept path; `is_ok()` has no variant to name (accept-path) |
| `crates/ken-kernel/tests/k4_omega_motive.rs` :: `non_type_non_omega_motive_still_rejected` | durable-invariant | high | Already checks the specific `KernelError::BadEliminator` variant, not just `is_err()` — the negative discriminator proving the Ω-extension is an addition (Type ∪ Ω), not a wildcard accept |
| `crates/ken-kernel/tests/ktr1_constructor_universe_gate.rs` :: `proposition_payload_at_family_level_is_accepted` | durable-invariant | high | Accept-path admission-gate test (Ω0 proof payload stays valid in a Type-0 family); no error variant to name since nothing is rejected here |
| `crates/ken-runtime/src/object_linker_packaging.rs` :: `shared_helper_wrapping_mutation_turns_generic_object_discriminator_red` | durable-invariant | high | Deliberate mutation/negative-control test: injects a wrong native-int lowering, asserts the pipeline fails — proves the positive packaging tests actually discriminate real correctness, not vacuous `is_err()` |

## foundation-qa (23 tests)

| test | class | confidence | note |
|---|---|---|---|
| `crates/ken-elaborator/tests/px8p_checked_buffer_producer.rs` :: `real_checked_buffer_bracket_emits_exact_allocate_and_release_sigma` | durable-invariant | high | Asserts a typed set equality on `export.alphabet` (BTreeSet) plus structural obligation/plan fields — a real vector, not a bare count |
| `crates/ken-elaborator/tests/sub1b_uint8_deceq.rs` :: `ac2_ac3_catalog_derivation_adds_zero_trust_and_has_real_laws` | durable-invariant | high | `trusted_base()` before==after is a relation (zero-delta), not a frozen count |
| `crates/ken-elaborator/tests/t1_acceptance.rs` :: `proved_result_empty_diagnostics_and_delta` | durable-invariant | high | Relational: proved verdict ⇒ empty diagnostics/delta; `obs.len()==1` is the fixture's own single-obligation input, not a census |
| `crates/ken-elaborator/tests/t1_acceptance.rs` :: `hole_kind_round_trips_and_lists_in_delta` | durable-invariant | high | Round-trip paired with structural assertions (kind=="hole", hole present in trusted_base_delta) — the honesty-guard invariant, not vacuous round-trip |
| `crates/ken-elaborator/tests/t1_acceptance.rs` :: `three_renderings_agree_one_source` | durable-invariant | high | Cross-walk relation across three independent projections of one verdict; the "one" in the name is the relation, not a quantity |
| `crates/ken-elaborator/tests/t1_acceptance.rs` :: `non_discharged_implies_non_null_diagnostic` | durable-invariant | high | Totality law: diagnostic:null iff discharged, checked both directions (discharged/open/refuted) — a real biconditional |
| `crates/ken-elaborator/tests/t1_acceptance.rs` :: `stable_field_drop_or_rename_rejected` | compat-vector | high | Pins required-field/drop-reject + unknown-field-ignored schema stability contract — a wire-format compatibility vector, changing it is a contract decision |
| `crates/ken-elaborator/tests/v4_acceptance.rs` :: `fully_proved_program_zero_diagnostics` | durable-invariant | high | Relational: all-proved obligation set ⇒ `project_all` returns empty, per `24 §7` AC5 |
| `crates/ken-host/src/effect_v1.rs` :: `all_pre_resource_operations_share_one_semantic_dispatch` | durable-invariant | low | Intent is "one dispatch path for all non-resource ops," but the assertion is a positional slice `HostOpV1::ALL[..15]` — silently wrong if `ALL`'s declaration order ever changes; recommend keying the invariant on set membership instead of position |
| `crates/ken-host/src/effect_v1.rs` :: `catalog_is_closed_and_availability_is_exact` | durable-invariant | low | Mixed test: the producer==registry==observer==consumers set-equality checks are sound durable invariants, but `assert_eq!(HostOpV1::ALL.len(), 22)` is the exact frozen-milestone-census anti-pattern our own playbook cites (the "nine native/thirteen unavailable" incident) — will go red on any legitimate catalog growth. Flag for split in rework |
| `crates/ken-host/src/effect_v1.rs` :: `generated_manifest_closes_catalog_observer_and_consumer_sets` | durable-invariant | high | Textbook closure check: four independently-derived sets (producer/registry/observer/consumers) asserted equal — exactly the pattern our playbook recommends |
| `crates/ken-host/src/effect_v1.rs` :: `canonical_payloads_and_unknown_identities_preserve_discriminators` | durable-invariant | high | Non-degenerate `assert_ne!` pairs per discriminating field (size, kind, name, family, raw_operation_id) — real discriminator coverage, not one positive case |
| `crates/ken-host/src/effect_v1.rs` :: `caller_control_release_invalidates_before_close_and_never_retries` | durable-invariant | high | Tests invalidate-before-close + exactly-once-retry via caller-injected error (linux); the doc comment itself is honest that this proves caller-control, not OS behavior |
| `crates/ken-host/src/effect_v1.rs` :: `resource_owner_and_close_allowance_are_structurally_confined` | UNCLASSIFIABLE | low | R4 case: asserts over `include_str!`-loaded Rust source text (`.contains`, `.matches().count()`) rather than the type system or the dispatched mechanism — brittle to any textually-different-but-behaviorally-identical refactor. Recommend Q3+ replace with a compile-time trait-bound check (no-Clone) and a real call-order assertion instead of string search |
| `crates/ken-host/src/effect_v1.rs` :: `distinct_host_roots_have_byte_identical_resource_lifecycle_observations` | durable-invariant | high | Determinism/no-path-leak invariant (linux): two independent temp roots produce byte-identical observation events; env-coupling (temp dirs) is incidental fixture plumbing, not the thing under test |
| `crates/ken-host/src/effect_v1.rs` :: `fs_open_attenuates_the_live_handle_to_the_requested_mode` | durable-invariant | high | Capability-attenuation semantics (linux): a READ-only handle rejects a METADATA op with the exact structured `RightNotHeld` error — real capability model law |
| `crates/ken-host/src/effect_v1.rs` :: `bounded_positioned_io_reaches_progress_mismatch_and_ordered_bindings` | durable-invariant | high | Exercises buffer-limit boundary (over-capacity rejected with typed `BufferLimit` error, linux), partial-read progress semantics, and exact resource-binding role ordering — structural, not timing-dependent despite the R5 flag (no wall-clock, just real fs fixtures) |
| `crates/ken-kernel/src/inductive.rs` :: `positivity_nat_accepted` | durable-invariant | high | Trust-root positivity accept case, direct arrow-domain, `is_ok()` — single error type so no variant to name |
| `crates/ken-kernel/src/inductive.rs` :: `positivity_bad_rejected` | durable-invariant | high | Non-degenerate reject pair to `positivity_nat_accepted` on the same mechanism (direct negative domain) |
| `crates/ken-kernel/src/inductive.rs` :: `positivity_bad3_in_application_rejected` | durable-invariant | high | The hidden-negative-in-application class COORDINATION §7 cites the Architect catching — load-bearing trust-root regression guard |
| `crates/ken-kernel/src/inductive.rs` :: `positivity_bad4_in_own_indices_rejected` | durable-invariant | high | Negative occurrence in own index telescope — distinct discriminating case from `positivity_bad3_in_application_rejected`, same soundness mechanism |
| `crates/ken-kernel/src/inductive.rs` :: `w_style_pi_bound_admitted_in_k1p5` | durable-invariant | high | Accept side of the K1.5 W-style admission (`14 §2.1/§8.4`) plus a structural check on `recursive_args` output (one Π-binder) — not a bare count, a shape assertion |
| `crates/ken-kernel/src/inductive.rs` :: `w_style_branching_domain_not_d_free_rejected` | durable-invariant | high | Non-degenerate reject pair to `w_style_pi_bound_admitted_in_k1p5` — same W-style mechanism, branching-domain-not-D-free polarity check; comment explicitly names the soundness guard this protects |
