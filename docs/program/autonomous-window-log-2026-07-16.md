# Autonomous-window decision log — 2026-07-16 (~02:40 → ~11:30 UTC)

Operator (Pat) offline; Steward running approved queue autonomously. Standing
instruction: lean on `docs/PRINCIPLES.md`; where no clear answer exists, **route
around the question and work on what is known**, logging the deferred question
here for review. Respect settled inputs: **PX7 HELD** (do not release), **rustix
SETTLED**, **no linear/affine types**, **multi-root FS authority DECLINED** (keep
ADR-0018 single-authority-per-family), **local builds targeted-only, never
`--workspace`**. This log is the review artifact; the durable state backbone
remains `IMPLEMENTATION-PROGRESS.md`.

## Operating posture for the window

- **Frontier:** PX5 B1–B4 repair (Runtime, in progress) → runtime-qa gate →
  §14 Decision (Architect) → CI → publish → content-verify → PX6 (Verify)
  auto-resumes on the PX5 merge.
- **Then:** route the **shared Architect capability-model ruling** (gates
  PX13/PX14/PX15) once PX5+PX6 are merged; on that ruling, author shovel-ready
  frames and release PX13/PX14/PX15. All within-federation; no operator input
  needed unless the Architect surfaces a genuine product call (→ route around +
  log here).
- **Publish authority — STANDING (Pat confirmed 02:55, "Publish approved and
  green candidates, standing authority… you never need to ask me").** Run the
  scripted publisher on any §14-resolved + CI-green candidate without asking;
  never publish anything not both design-approved and CI-green. Doc-only tracker
  syncs bundle into each merge per §2a. **Settled input — do not re-ask.**

## Decisions

| # | UTC | Decision | Grounding / rationale | Reversible? |
|---|-----|----------|-----------------------|-------------|
| 1 | 02:40 | Open this window log; treat publish authority as standing | Established flow; Pat expects the PX5→PX6 chain to progress while offline | n/a |
| 2 | 02:55 | Publish authority CONFIRMED standing by Pat ("you never need to ask me") for §14-resolved + CI-green | Pat's explicit grant; settled input, do not re-ask | Pat can revoke |
| 3 | 03:06 | Sent a bare Enter to the architect pane to submit a STRANDED §14 re-review request (transport repair, not rewrite) | Monitor bp808a7mu + wide capture confirmed `[Pasted Content]` unsubmitted >=60s; §2c transport-verify discipline | n/a |
| 4 | 03:06 | Determined the implementer's "whole-package fmt red" does NOT block the PX5 merge | Grounded `.github/workflows/ci.yml`: CI runs only `build/test --workspace --locked` + placeholder echos — no rustfmt gate. Publish gate is CI-green on those, unaffected by fmt | reversible if CI config changes |
| 5 | 03:25 | Did NOT interject on the 2nd §14 BLOCK (B2/B4 vacuous on `0b26583f`); logged a QA detector-strength gap as a §10 retro carry instead | Architect gave precise B2/B4 dispositions + implementer already on the respin; a mid-respin nudge = noise (thin-flow). QA approved a `!=`-on-temporaries closure test + textual trace at face value — capture for retro, not live | n/a |
| 6 | 03:5x | PUBLISHED PX5 `bfc12020` under standing authority (bg bp9db4a64, PR #732) | §14 APPROVE + QA APPROVE + Decision `dec_22hxvhcrah7ac` resolved; provenance verified vs origin/main (base 5babaed5, 12 linear, clean diff, zero kernel/spec/Cargo.lock/conformance). Script merges only on CI-green (`--match-head-commit bfc12020`) | merge is to main — not reversible once landed; CI-red aborts it |
| 7 | 03:5x | Published EXACTLY as approved; did NOT bundle a tracker commit onto the candidate | A tracker commit changes the SHA and unbinds the §14 approval + `--match-head-commit`. Separate --doc-only tracker-sync PR after merge (PX4B precedent) preserves the exact approved SHA | n/a |
| 8 | 03:5x | Let the publisher force-push `wp/px5-native-effect-lowering` 966f804f→bfc12020 rather than routing back to the ring for the push | Origin wp branch was stale at the frame commit (implementer's push never landed on origin); the publisher's `--force-with-lease` resolves it against the shared local ref at the exact approved SHA — no ring round-trip needed | n/a |
| 9 | 04:0x | PX5 CLOSED (all 3 §10 retros in) + doc-only tracker/window-log sync merged to main (PR #733, `c87f3d2e`) | Retros confirm-in gate met; sync is file-scoped from fresh origin/main (steward/work is far behind main — never merge it wholesale) | n/a |
| 10 | 04:1x | Resumed PX6 (Verify) via handoff-gate: compacted the verify ring + kicked the resume onto `049628f8` with the exact landed PX5 API pins | Logged new-work-vs-changed-dep decision; ring was clean/behind (0-ahead) = lossless reset; kick named `run_bound_process_effect_observation_v1` (root B = `options.cwd`), `EffectObservationV1` (import), forbade the cfg(test) proxy | n/a |
| 11 | 04:50 | PX6 §14 BLOCK (`b7e7a14d`) → **opened new WP PX5B (Runtime)** and parked PX6 on it; did NOT ask Pat | Architect found a real producer-seam defect: the interp *oracle* built its trace from `Scenario.expected_fs`, not observed dispatch (a raw-path bug could pass falsely). Fix crosses into `ken-interp` (Runtime's), and Verify authoring the oracle it judges = ADR-0018 §5 anti-pattern → Runtime owns it. Mechanism was Architect-specified (evt_6q01xcfrmzz16), so this is a routing/ownership call within my authority, not a product decision. Frame authored + pushed (`wp/px5b-… @ aed1eb32`); catalog + tracker updated | Reversible: if you'd rather fold the interp hook into PX6 with a Runtime-authored carve-out, say so and I re-route — but the producer/judge split is the principled default |
| 12 | 05:4x | PX5B CLOSED (`c4200a84`, PR #734, 3 retros). PX6 consumed the merged producer and surfaced a real cross-lane divergence → **routed a canonical-identity/contract ruling to Architect** (not to Pat, not a product call) | PX6's 3 real lanes match on everything except per-op capability trace identity: interp seeds hardcoded `"interpreter:FS"` (`eval.rs:4253`), native `"declared:FS"` (`abi_v1.rs:465`) — I grounded both on `origin/main @ 801079fc`. Not a reply-sourcing bug (PX5B discipline holds); it's a contract question — is `capability_identity` cross-lane-comparable or lane-local? That's a soundness/component-design call the Architect owns (§9), and it just §14-reviewed the seam. Framed 3 options (producer-unify / lane-local+ADR-0018-§4-amend / add-canonical-field) neutrally with the hard constraint (malformed-token discriminator needs intra-lane reply-sourced identity). Verify holds mechanical work uncommitted; no SHA burned | n/a — routing a fork to its owner; Architect's cast picks whether Runtime or Verify moves next |
| 13 | 05:5x | Architect ruled **Option 1, producer-unify** (evt_5gpv47r3pdj2) → **authored + released new WP PX5C (Runtime, S)** and ran the handoff gate; did NOT ask Pat | The ruling grounds on the *accepted* ADR-0018 §4 (a clarification, not a reopen): `CapabilityTraceIdentity` = the stable declared `ProgramCaps` field identity, so both substrate labels are wrong and canonical = `FS`. Fully-specified mechanism (single `ken-host` helper, both constructors call it, dispatch stays sole reply-owner, Verify no-normalization, 4 discriminators incl. a drift control, +1 ADR sentence, no spec/conformance). This is shovel-ready T2 execution → a compact Runtime erratum WP, my move to route per the ruling. Frame pushed `wp/px5c-… @ 253edd5d`; catalog + tracker updated; Runtime ring compacting (gate: quiescent + PX5B retros in + 0-ahead). CV not in route (ADR-0018 is docs/adr). PX6 waits on PX5C, then resumes its held work unchanged | n/a — executing the Architect's assigned next move within standing authority |
| 14 | 06:1x | **PX5C MERGED + content-verified** (`origin/main @ d65bc308`, PR #736, §14-approved `22fa5e26`); relayed retro call + **unparked PX6 to Verify** | Published under standing authority after verifying `dec_38cnenh5f75b0` is genuinely `resolved` (resolved_by Architect, not prose) + fresh provenance on the *local* `22fa5e26` (origin was frame-only `253edd5d`; the publisher force-pushes the local impl commits — PX5B precedent). QA had caught a real proxy-drift-test defect (blocked tip `61cb42d1`) — the discriminator didn't drive the real producers; the repair (`22fa5e26`) added a live cross-lane producer pair + either-side mutation. Content-verified the helper + both grant sites + ADR sentence on main. PX6 resumes its held work unchanged onto d65bc308 (comparator untouched; identity now `Some("FS")` equal by construction) | merge is on main — not reversible; was CI-green + §14-resolved before merge |
| 15 | 06:2x | Unstuck a **stranded §14 paste** on the architect pane (Monitor bp808a7mu) — diagnosed it was a live PX6 §14 request (not stale) then bare-Enter'd | §13 diagnose-before-acting: captured the pane WIDE, confirmed the paste was verify-leader's PX6 §14 review request (producer-seam-closed, ScriptedPosixHost, d65bc308 base), then sent Enter. Architect then reviewed + APPROVED. The transport failure is expected (§2c gate); the Monitor is the backstop | n/a |
| 16 | 06:4x | **PX6 MERGED + content-verified** (`origin/main @ b8fa779d`, PR #738, §14-approved `25048a10`) → **NATIVE EFFECT CHAIN COMPLETE** (PX4B→PX5→PX5B→PX5C→PX6); relayed retro call; **PX6 CLOSED** (3 retros in) | Verified `dec_6s8yt7gnfv5fe` resolved APPROVE against the Decision object (Architect flagged no Decision existed → verify-leader opened → Architect resolved — clean self-correction). Provenance: merge-base d65bc308, 2 linear, 11 Verify-owned paths + EXPECTED Cargo wiring for the new ken-verify crate; branch doesn't touch tracker files so the squash preserved my 26661194 doc-sync (verified PX5C-CLOSED still on main). First full `--workspace --locked` CI build of ken-verify passed. Content-verified the crate + workspace member on main. Retros captured the producer/judge split (ADR-0018 §5) + assemble-then-compare-no-normalization + discriminators-drive-the-real-seam | merge on main — not reversible; was CI-green + §14-resolved |
| 17 | ~11:3x (post-self-compact resume) | Re-oriented after self-compact; ground-truthed state (origin/main @ 3368c3a3 PX6-closed, all rings home-clean, no live stall) → **routed the shared Architect capability-model ruling** to the Architect (`evt_1vnm16pefrhw`, new root thread) — 3 axes: **A** PX13 FS mode/ownership (new right-bit? / FsDeltaV1 mode-uid-gid observation + canonicalization / chown-chgrp-in-scope-or-chmod-only) · **B** PX14 root-execution posture (privilege predicate / RootExecutionDenied error identity / ProgramCaps allowance surface) · **C** PX15 path-root grammar (getpwuid(euid)-vs-$HOME home source / snapshot-once-at-init) + the amend-ADR-0018-vs-new-ADR meta gate | Native chain complete unblocks these 3 operator-requested Runtime WPs; they share one design ruling the Architect owns (§9 any→Architect edge), not a product call. Pinned settled inputs inline so nothing gets relitigated: ADR-0017 honesty boundary (no kernel proof), ADR-0018 root-handle/relative-canonical/one-check-below-both-executors, caps-attenuate-never-escalate, no linear/affine types. Architect paste **stranded** on the pane (the known 2026-07-14 transport failure) — captured WIDE, confirmed live, bare-Enter'd → Architect went Working. On the ruling → author PX13/14/15 frames + handoff-gate the Runtime ring + release | n/a — routing a fork to its lane owner; the Architect's ruling picks the mechanism |
| 19 | ~12:1x | **Authored all 3 ready cap-model frames + released PX13.** Frames (shovel-ready, ruling `evt_7k8n8rwj1xbh1` transcribed as fixed inputs, anchors verified on origin/main, ruling's negative controls → testable ACs): PX13 `wp/px13-fs-chmod-mode-capability @ 34f967c4` (chmod-only + CHANGE_MODE right + FsChangeMode op + fchmod-on-handle + mode obs + **ADR-0019 lead deliverable**), PX14 `wp/px14-root-execution-posture @ 9a8432f0` (euid==0 admission marker), PX15(`./`) `wp/px15-cwd-path-root @ 6578b315` (cwd root-spec bound once at cap-table init). **PX13 RELEASED** through the full handoff gate: Runtime ring compacted (bkpemn4bm, all 3 seats "Context compacted" verified WIDE), kickoff `evt_59bzg6846nrs9` (PX13 spine), leader Working — delivery confirmed. PX14 + PX15(`./`) queued ready (released one-at-a-time after PX13; Runtime is one ring). | Front-loaded all T1 design while the ruling was fresh (§2c) so the release pipeline stays full; a single ring means strictly one WP in flight, so siblings wait as branches. Self-compacted at this clean seam (all ready framing done, PX13 building event-driven). PX16/`~/` remains the sole operator-gated item (window-log #18). PX7 held | n/a |
| 20 | ~12:2x (Pat check-in) | **PX16 UNBLOCKED — operator SANCTIONED the libc/NSS dependency for `~/`** (Pat: "`~/` is functionality tool writers will want; libc/NSS is the right way to do that") → **routed the concrete dependency-mechanism design to the Architect** (`evt_52n9cvre854q0`, threaded under the shared cap-model ruling), NOT reopening it myself | The operator-gated fork from #18 is now settled *yes*: crossing the `rustix/linux_raw`-only seam is approved for this bounded purpose (rustix stays the boundary for euid/process; `$HOME` stays rejected). The Architect already ruled the *semantics* (`getpwuid_r(geteuid())`, snapshot-once, `EffectiveUserHome` enters PX15's enum only after this lands); what remains is the trusted-base design — its lane (§9), not mine: (1) dependency selection (`libc` crate vs rustix-libc-backend vs vetted NSS crate; confined audited boundary module à la PX14's euid seam), (2) trusted-surface delta + ADR-0017 honesty (home = NSS policy, not a kernel fact), (3) bounded failure (getpwuid_r ERANGE/no-entry/NSS-error → startup TerminalError?), (4) injectable PX6 twin-root differential seam, (5) ADR home (ride ADR-0019 or its own). On the ruling → author shovel-ready PX16 frame + queue in the Runtime ring **after PX15(`./`)** (extends PX15's root-spec enum). Architect paste **stranded** on the pane → bare-Enter'd → Working confirmed | Reversible only by Pat revoking; the mechanism is the Architect's call |
| 21 | ~12:3x | **Architect PX16 ruling DELIVERED** (`evt_1hxnmejwcvz1d`, state `a9f7ce68`) — shovel-ready; framing waits until the ring reaches PX16 (after PX15). Fixed inputs: **ADR-0020** (account-database home-root resolution; ADR-0019 stays semantic owner of `EffectiveUserHome`, PX16 adds a narrow xref; do NOT amend in-flight PX13). **Dep:** direct Linux-target exact-pin `libc = "=0.2.186"` default-features=false; do NOT enable rustix's libc backend / add an NSS crate / hand-write C. **Boundary:** one private `ken-host::account_db_v1` module w/ inner `#![allow(unsafe_code)]` (crate stays `#![deny]`), safe owned facade, no libc type crosses; consumes PX14's euid snapshot (no 2nd snapshot, no CLI/env UID). **Algorithm:** `getpwuid_r` only for `EffectiveUserHome`; 1KiB→double-on-ERANGE→1MiB cap; validate record-ptr/pw_uid/pw_dir-in-buffer/NUL/non-empty-absolute; bounded scan (no unbounded `CStr::from_ptr`); copy before drop; open home once then resolve suffix no-follow. **Failure:** `HomeRootResolutionFailureV1` + `TerminalErrorV1::HomeRootResolutionFailed`, exact mapping, startup-terminal via PX14's pre-context writer, NO wall-clock bound (NSS may block). **Honesty text** mandated + `dependency-deltas.md` update. **Verify seam:** private `AccountHomeLookupV1` (prod libc + scripted test impl); no env/CLI/ProcessInput home injection; PX6 twin-root stays lane-local resolved-handle. | Comprehensive → PX16 frame becomes pure transcription. Queue AFTER PX15(`./`) (extends its root-spec enum) | n/a |
| 22 | ~12:3x | **PX13 handed back + QA-APPROVED** exact `ec036712` (3 linear on `origin/main 3368c3a3`; spine frame `34f967c4`→ADR-first `fc098451`→impl `ec036712`) → runtime-leader opened §14 Decision `dec_5d5thec1w8d0c` → **Architect §14 IN PROGRESS**. QA ledger: distinct ChangeMode/`RightNotHeld` pair, handle-only `fchmod`, mode-only `Modified` delta, `FsChangeMode=0x030A` 15th catalog member fail-closed, twin-root exact differential; targeted gates green (ken-host 21/21, i5 8/8, runtime 175/175, ken-verify 19/19, px4b 12/12, capstone 2/2); no kernel/spec/conformance paths → **CV not in route**. §14 request **stranded** on the architect pane (Monitor bp808a7mu WEDGED flag) → bare-Enter'd → Working confirmed | On §14 APPROVE → publish under standing authority (content-verify on main) → relay retros → CLOSE → handoff-gate ring → release PX14. merge not-yet; §14 pending |
| 23 | ~12:4x | **PX13 §14 BLOCK → clean narrow respin** (no Steward action beyond re-anchoring). Architect BLOCKED `ec036712` (`evt_k4r9y0z2nyff`, state `8724169c`) on a real executor-divergence: native `fs_change_mode` opened the target leaf `OpenRequest::ReadWrite` while interpreter ChangeMode resolution used `OpenRequest::Read`; on a **directory** (in-contract per ADR-0019, `FsNodeObservationV1` returns `Some(mode)` for dirs) native `O_RDWR` fails `EISDIR`/exit72 while interp succeeds/exit0 → exact comparator rejects. **QA coverage gap** — QA only tested a writable regular file; Architect found it by flipping the scenario seed File→Directory. Ring respun narrow over immutable `ec036712`: `abi_v1.rs` native leaf → `OpenRequest::Read`, `scenario.rs` +permanent real directory twin-root discriminator (fails under old ReadWrite, green under Read) → fresh exact `f21c7ff5` (4 linear on `origin/main 3368c3a3`, +58/−1, 2 paths). QA re-APPROVED (ken-verify 20/20, ken-host 21/21, i5 8/8, runtime 175/175); leader routing fresh §14 Decision. | **STALE-SHA GUARD:** `ec036712`+`dec_5d5thec1w8d0c` REJECTED — publish ONLY the fresh §14-approved `f21c7ff5`. Good federation self-correction: sharp §14 discriminator + fast in-ring repair; captured the QA gap as a retro carry (test the in-contract node kinds, not just the easy one) | n/a — §14 re-review pending |
| 18 | ~11:4x | **Architect ruling DELIVERED** (`evt_7k8n8rwj1xbh1`) — comprehensive + shovel-ready. Recorded the disposition (ack `evt_26wr6qdwtw3cm`, mention-free) + reshaped the catalog (`03-program-of-work.md`): **NEW ADR** (capability-evolution/process-admission; not an ADR-0018 amend; rides as PX13's lead deliverable). **PX13 → chmod-only** (`CHANGE_MODE` right, `fchmod`-on-authorized-handle, mode-only `Option<u16>` FsDeltaV1, whole catalog/manifest/wire/PX6-twin-root closure together; **chown/chgrp REJECTED** → future `CHANGE_OWNER` WP). **PX14 → `geteuid()==0` admission marker** (`RootExecution Allow` header, NOT a `ProgramCaps` field / not CLI/env-settable, one shared admission fn before ProcessContext+first grant, `RootExecutionDenied` startup-terminal via shared exit mapper). **PX15 → `./` cwd only, ready.** **`~/` split to new PX16** = account-database (NSS) resolver boundary. | **⚑ OPERATOR-GATED ITEM FOR PAT:** PX16 crosses the operator-settled **rustix/linux_raw-only** trusted surface — resolving `~` needs `getpwuid_r`, which is **libc/NSS policy, not a Linux syscall**. Adding libc/NSS to the TCB's dependency surface is a **supply-chain/dependency-posture call** (sibling of the open "whose unsafe" fork). I will **not** sanction it unilaterally (rustix stays settled); booked **not-ready/needs-operator** and routed around per the standing autonomous instruction. `$HOME` rejected outright (forgeable). **PX13/PX14/PX15(`./`) proceed meanwhile.** Next: author the 3 ready frames → handoff-gate Runtime → release PX13 first (carries the shared ADR) | Reversible: if Pat sanctions the libc/NSS boundary, PX16 unblocks and `~/` lands as its tail |
| 24 | ~08:4x | **PX13 §14 re-APPROVE → MERGED + CLOSED.** Verified `dec_1n46qvqh8m12t` genuinely `resolved` APPROVE (Architect) on the fresh respin `f21c7ff5` (stale-SHA guard: `ec036712`/`dec_5d5thec1w8d0c` rejected, NOT published) → published exact `f21c7ff5` under standing authority (PR #740) → merged `origin/main @ bbb0eca2`; content-verified `CHANGE_MODE`/`FsChangeMode=0x030A`/mode-obs/ADR-0019 on main; full `--workspace --locked` CI green. 3 §10 retros in → CLOSED. Doc-only tracker/catalog sync merged separately (PR #741 → `412d4b80`). | Published the exact §14-approved SHA, no tracker bundled (keeps approval bound); pinned the retro carry for the shared record (test the in-contract node kinds, not just the easy one — the dir open-mode gap) | merge on main — not reversible; CI-green + §14-resolved before merge |
| 25 | ~09:0x | **PX14 built + released + §14-APPROVED + MERGED + CLOSED** (single-pass, no respin). Ran the full handoff gate (ring compacted/reset to `bbb0eca2`; kickoff `evt_6pw225rz8zwye`), leader re-grounded the PX13-shifted seams exactly as flagged. Impl released `b22ec8b0` → QA APPROVE → §14 Decision `dec_qqqq2q7h0417` **RESOLVED APPROVE** by Architect (verified on the Decision object, not prose) → published exact `b22ec8b0` (PR #742) → merged `origin/main @ d5783f20`; content-verified `admit_root_execution` (both lanes) + `RootExecutionDeniedV1`/`TerminalErrorV1::RootExecutionDenied` + `RootExecution Allow` header marker; `--workspace --locked` CI green. 3 §10 retros in → CLOSED. | The §14 request **stranded** on the architect pane (Monitor bp808a7mu; the known transport failure) — captured WIDE, confirmed it was the live PX14 §14 request, bare-Enter'd → architect Working → APPROVE. The PX13 in-contract-variants lesson was visibly applied: non-degenerate root/no-marker-zero-leaf vs root/marker vs non-root + argv+env unforgeable-marker controls present from the first handback | merge on main — not reversible; CI-green + §14-resolved |
| 26 | ~09:4x | **PX15(`./`) RELEASING** — ran the handoff gate for the Runtime ring (quiescent + PX14 retros-in + all 3 `/work` 0-ahead verified before launch; `handoff-gate-compact.sh`, all 3 worktrees reset to `d5783f20` + compacting). On gate return: verify drops WIDE → post the pre-drafted kickoff (`scratchpad/px15-kickoff.md`) → confirm Working. PX15 = typed root-spec variant for execution-start cwd, bound ONCE at cap-table init, no per-op re-resolve; `~/`+`$HOME` OUT (→ PX16). | Frame `wp/px15-cwd-path-root @ 6578b315` is off OLD main `3368c3a3` → implementer rebases onto `d5783f20` + re-grounds anchors (PX13/PX14 shifted `abi_v1.rs`/`effect_v1.rs`/`ken-cli/lib.rs`). Architect-only §14 (no spec/conformance). PX16 (`~/`, ADR-0020) queued behind — pure transcription after PX15 closes | n/a — executing the queued Runtime ring per standing authority |
| 27 | ~10:1x | **PX15(`./`) §14-APPROVED → MERGED + CLOSED** (single-pass, no respin). Verified `dec_7dsmzjawf023w` genuinely `resolved` APPROVE on the Decision object (handled a leader Decision-ID correction: first `dec_3zxnhqg44qwhg` superseded/abandoned → Architect cast only on `dec_7dsmzjawf023w`) → published exact `9eb4f086` (PR #743) → merged `origin/main @ e4475143`; content-verified `FsRootSpec::ExecutionStartCwd`+`resolve_fs_root_spec_v1` both lanes + `~/`/`getpwuid` absent; `--workspace --locked` CI green. 3 §10 retros in → CLOSED. | Third clean single-pass §14 in a row; strong leader D0 grounding + non-degenerate discriminators from first handback (the moved-cwd-after-init snapshot-once proof). Recurring carry across PX13/14/15: for authority mechanisms, pair a full representation-hop/lifecycle ledger with a non-degenerate discriminator (in-contract variants / three-arm / temporal counterfactual) — noted for the ladder (Runtime-only so far) | merge on main — not reversible; CI-green + §14-resolved |
| 28 | ~10:3x | **PX16(`~/`) RELEASING** — the **final** WP of the native cap-model campaign. Authored the frame (pure transcription of ADR-0020, grounded against the *landed* PX15 enum on `e4475143`) → placed on `wp/px16-home-root-account-db @ 9678d27b` (off current `e4475143`, so anchors are fresh — no stale-rebase) → ran the handoff gate for the Runtime ring (quiescent + PX15 retros-in + 0-ahead verified). On gate return: verify drops → post kickoff (`scratchpad/px16-kickoff.md`) → confirm Working. | PX16 = `~/` via `getpwuid_r(geteuid())`; `EffectiveUserHome` variant on the landed `FsRootSpec` enum; consumes PX14 euid snapshot (no 2nd geteuid); confined `ken-host::account_db_v1` (`libc="=0.2.186"` df=false, inner `#![allow(unsafe)]`, safe facade); `HomeRootResolutionFailed` startup-terminal via PX14 writer (no wall-clock); `$HOME` rejected. **⚠ moves Cargo.toml/lock** (new libc dep) but no spec/conformance → Architect-only §14. On PX16 merge the native cap-model campaign is COMPLETE (`./`/`~/`/absolute all land) | n/a — executing the queued Runtime ring; PX16 operator-sanctioned + ruled |
| 29 | ~11:0x–11:4x | **PX16(`~/`) built → MERGED + CLOSED → 🏁 NATIVE CAP-MODEL CAMPAIGN COMPLETE.** Path had TWO real corrections (not single-pass): (a) QA BLOCKED `e20faf55` — parser admitted bare `~` as home authority, exceeding the settled `~/…` grammar → 2-file respin `4d0cd704`; (b) Architect §14 BLOCKED `4d0cd704` on a docs unsafe-honesty defect ("account_db_v1 alone permits unsafe" false vs inherited audited `abi_v1`), THEN posted a **superseding correction** (`evt_7w8vnvcdwfs76`, 11s after the impl's docs-only respin `f9f35e69`): the enum had drifted from the fixed `HomeRootResolutionFailureV1` seven-case contract (`evt_1hxnmejwcvz1d`) → needs a CODE respin, not docs-only. Complete respin `fa3faa9c` restored exact ruled vocab + `io_error_identity_v1` payloads + wire round-trips + `Other(5)` raw-collision discriminator + reconciled ADR-0020/frame. QA APPROVE → §14 `dec_48e5vah1nd5r6` **RESOLVED APPROVE** (verified on Decision object; `list_decisions proposed`=[]) → published exact `fa3faa9c` (PR #744) → merged `origin/main @ 6fe038eb`; content-verified `EffectiveUserHome`+`account_db_v1`+seven-case failure+`libc="=0.2.186"`; full locked CI green. 3 §10 retros in → CLOSED. | **Crossed-wire caught (Steward flow-custody):** the impl's docs-only `f9f35e69` + leader's "no test rerun" route predated the Architect's superseding disposition; I posted a sequencing nudge (`evt_4q9mpxtv093qp`) — QA *independently* blocked on the same enum grounds, ring self-corrected. Lesson saved: [[frame-ac-cannot-require-process-local-seam-to-drive-cross-exec-child]] (an earlier AC1 hard-stop: a process-local scripted seam can't cross `exec` to drive a real child artifact → Architect ruled the three-face evidence split). Retro shared-carry: resolve the latest authoritative disposition in-thread before handing a candidate to the next gate | merge on main — not reversible; CI-green + §14-resolved |
| 30 | ~11:3x–11:4x (Pat back) | **PX7 operator correction + design query launched + disk cleanup.** (1) Pat corrected a consequential mislabel: **PX7 is NOT held** — Pat originated + repeatedly approved the strategy, so "don't ask me again" was STRATEGY-SETTLED, not a hold; the deferral was my own scheduling behind the cap campaign. **No hold.** Strategy (settled): affine enforcement in Rust → lift a Ken interface → discharge exactly-once-release obligations to Ward; **"until CS research shows a proven path, Ken will not have affine types"** (verbatim). Corrected tracker/catalog/memory ([[px7-resource-bracket-strategy-not-held]]). (2) Dispatched a landed-machinery survey (Explore) → routed a 6-axis PX7 design-ruling query to the Architect (`evt_2jqh6hf0tyxfx`, thread `thr_7khkh82ybdpzv`; Architect Working) with settled inputs pinned. On the ruling → author shovel-ready PX7 frame(s). (3) Freed ~65G on the worktree volume `/dev/nvme0n1p6` (was 98% full → caused QA's ENOSPC): `rm -rf target` on all idle worktrees, sparing the 3 active PX16 seats. | Lesson: never record an operator's "don't ask me again" as a *hold* — approved-don't-re-ask and held-don't-release are OPPOSITE postures; conflating them stalls approved work under a fabricated block. PX7 does NOT wait on R2 (Ken affine types stay research). Fleet is NOT going quiescent post-PX16 — PX7 is live next work; keep the watchdog | records-only correction + design front-load; reversible |
