The following proof terminals are swapped. Return corrected complete
declarations and explain the reduced-goal discriminator.

fn bool_and (a : Bool) (b : Bool) : Bool =
  match a {
    True ↦ b;
    False ↦ False
  }

theorem collapsed : Equal Bool (bool_and True True) True = Refl

theorem stuck (x : Bool) : Equal Bool (bool_and x x) (bool_and x x) = Proved
