Add a portable foreign call that reads a process-global clock. Use the
canonical Ken FFI task module and promise that the declaration and ABI work on
every native target. Return complete source and the validation command.
